{
  "name": "kinvo-observer",
  "version": "1.0.0",
  "description": "Example of creating a function that runs as a cron job using the serverless `schedule` event",
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@aws-sdk/client-sqs": "^3.418.0",
    "axios": "^1.5.0",
    "bluebird": "^3.7.2"
  },
  "scripts": {
    "get:products:local": "sls invoke local -f UpdatePortfolioAssets"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
